import { expect } from 'chai';

describe('Creature Tools', () => {
    it('should be implemented in future', () => {
        // Tools system removed for now
        expect(true).to.be.true;
    });
});
